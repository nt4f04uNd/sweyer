package com.nt4f04und.sweyer.sweyer_plugin

object Constants {
    const val LogTag = "com.nt4f04und.sweyer"

    enum class Intents(val value: Int) {
        FAVORITE_REQUEST(0), PERMANENT_DELETION_REQUEST(1);
    }
}
